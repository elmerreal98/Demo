version: 2.1
jobs:
  aws-cli-example:
    executor: aws-cli/default
    steps:
      - checkout
      - aws-cli/setup:
          profile-name: example
      - run: echo "Run your code here"
orbs:
  aws-cli: circleci/aws-cli@1.18.147
workflows:
  aws-cli:
    jobs:
      - aws-cli-example:
          context: aws
